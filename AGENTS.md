# BURNLAMP 仓库工作约定

本文件是 Codex 和其他编码助手进入仓库后的第一执行入口。用户当轮明确要求始终高于本文件；本文件用于补足稳定的项目背景、默认流程和验收边界。

## 开始前必读

按以下顺序读取：

1. `docs/DESIGN-PRINCIPLES.md`
2. `docs/CODEX-WORKFLOW.md`
3. `CONTENT-GUIDE.md`
4. 与当次任务直接相关的 `content/*.json`、组件和测试
5. 遇到环境、第三方资源或发布问题时，再读 `docs/KNOWN-ISSUES.md`

不要只凭聊天上下文或旧截图推断当前代码和正式站状态。

## 三个最高设计宗旨

- **高级**：结果要像为 BURNLAMP 的真实内容专门设计。高级来自信息层级、比例、排版、材质和边界状态，不是换色、加阴影、拉长动画或套用模板。
- **智能**：功能完整、语义清楚、状态可恢复、操作可打断。默认考虑键盘、触屏、慢网、失败、快速连续操作、减少动态、深链接、前进／后退和内容规模增长。
- **大气**：让真实内容成为主角，用舒展的尺度、坚定的主次、从容的留白和克制的动效建立冲击力。大气不等于所有元素放大或大幅运动。

三者不应相互牺牲。视觉和功能发生冲突时，先简化结构，再使完整功能在清楚的界面中成立。

## 稳定的项目事实

- 正式站：`https://burnlamp.is-my.id/`
- 仓库：`burnicelamp/burnicelamp.github.io`，GitHub Pages 从 `main` 根目录发布。
- 技术：原生 HTML / CSS / ES Modules；访客端无框架、无打包步骤。
- 内容：`content/*.json` 是真实内容单一来源；`content/search-index.json` 和 `content/relation-index.json` 是生成物。
- 导航：稳定 ID、URL 深链接、搜索、复制链接与浏览器历史都是公开契约。
- 域名和 SEO：保留 `CNAME`、`.nojekyll`、canonical、`robots.txt`、`sitemap.xml` 与站点元数据。
- 开发依赖：Sharp 和 Playwright 只用于维护、图片处理和验收，不进入线上运行时。

## 默认执行顺序

1. **确认源头**：确认当前目录是否有 `.git`，检查 `git status`、远端 `main` 和用户未提交修改。如果只有 GitHub 压缩包快照，明确记录，不冒充可正常提交的 Git 工作树。
2. **建立基线**：实际打开正式站，至少检查一个桌面宽度和一个手机宽度；记录真实加载、交互和控制台状态。
3. **只改任务范围**：保留既有色彩、字体气质、空间叙事和其他板块。允许修改必要的共用实现，但不借机重做全站。
4. **先结构，后装饰**：先解决内容层级、画面比例、导航语义、响应式和可达性；再调材质、阴影和动效。
5. **真实内容验证**：正式内容不用测试夹具代替。大量作者、长标题、长正文、失败资源只放在测试内存或路由夹具中。
6. **分层验证**：先运行与改动直接相关的检查，再运行 `npm run check`；视觉、导航、动效或内容结构变更运行 `npm run check:full`。
7. **交付审阅材料**：使用真实内容产出桌面／手机截图；动效和连续交互变更需短录屏，不用静态截图宣称过渡流畅。
8. **发布需明确授权**：用户本轮写明“部署”“发布”“推送”时，在验证通过后更新 `main`。否则交付本地可审阅结果，不改正式站。
9. **发布后复验**：确认 Pages 任务的 SHA 与本次提交一致，然后对正式域名再做桌面／手机、关键交互、网络请求和控制台检查。

## 内容与权利边界

- 不编造个人评价、阅读／观看状态、评分、精确地理位置或未提供的人生经历。
- 保留已有稳定 ID、条目顺序、出处、来源、搜索字段和深链接。
- 外部影视图片继续记录真实来源、署名、原始尺寸和使用方式。HTTP 200 只证明当次可达，不证明永久可用或拥有再分发权。
- 不提交原图、导入清单、未授权媒体、密钥、token、私密授权证明或带 EXIF/GPS 的源照片。
- 第三方嵌入的 `load` 事件不等于可播放；只有真实媒体时钟才可驱动进度、播放状态和同步歌词。

## 样式与交互约束

- 不在样式表末尾持续追加相互覆盖的补丁。先找到组件的唯一样式入口，删除或收敛历史规则。
- 桌面和手机是同一信息架构的两种布局，不用手机端隐藏问题来假装解决。
- 触摸横滑只负责一种切换语义，必须与页面纵向滚动共存。
- 动画默认短、轻、可打断；新状态完成加载和解码后再交接，快速连续操作以最新意图为准。
- `prefers-reduced-motion` 关闭不必要的运动，但不能丢失功能、状态或内容。
- 自定义目录和弹层必须有键盘操作、焦点进入／归还、Escape 关闭和可理解的辅助标签。

## 验收底线

- 宽度至少覆盖 320、390、768 和 1440px；大布局调整额外检查 980px。
- 覆盖长标题、长作者名、长正文、无摘句、空结果、首尾条目和大规模目录。
- 验证键盘、触屏、减少动态效果、深链接、前进／后退、慢网／失败恢复。
- 无新增未捕获控制台错误、本地资源 404、水平溢出、正文裁切、嵌套滚动和内容／图片错配。
- 第三方资源要区分“代码降级正常”与“外部服务当前可用”，不用本地替身证明正式服务成功。

## 常用命令

```powershell
npm install
npm run dev
npm run preflight
npm run check
npm run check:full
npm run verify:cinema-media
npm run review:capture
npm run review:record
```

命令用途、Windows / Playwright 环境变量和发布降级方案见 `docs/CODEX-WORKFLOW.md` 与 `docs/KNOWN-ISSUES.md`。
