# 本次回归记录

2026-09-10，基于 main e7cb32de27be019e5f722a2b09702a5fe4913ee5。

通过 node tests/content-check.mjs 与 Edge / Playwright 浏览器回归：

- 六份独立 JSON、唯一 ID、素材路径、歌词授权与时间轴边界；页面不含曲目或条目正文。
- 五首原曲目、顺序、封面地址、Apple 官方嵌入地址与平台入口保持一致；曲目和歌词视图联动。
- 320 / 390 / 768 / 980 / 1440px 无页面横向溢出，辅助文字不低于 12.5px，顶部移动导航可关闭、支持 Escape / 焦点和当前板块。
- 原有影像前后切换、全部书页及回翻、肖像方向键切换、照片控制。
- 随机浏览仅访问实际已发布内容，不连续重复，定位后不被页头遮挡；直接链接可展开对应电影、书页、肖像和记录。
- 临时追加一条肖像及三张照片、一本书、一部电影、一条记录，无需修改 HTML 或组件。
- 原创测试文字与内存生成静音音频：真实播放、暂停、跳播、倒退、片段偏移、逐句高亮、手动滚动后 5 秒恢复、不滚动外层页面、曲目切换清理。
- 有授权文字但无播放时钟时自由阅读；无可展示歌词时优雅降级；歌词权限不完整或已过期时不展示。
- prefers-reduced-motion 下无平滑滚动或装饰运动；内容 fetch 失败只影响对应板块，不引发未捕获异常。
- 数据字符串按文本输出，测试 HTML 字符串不会成为可执行标记。

桌面和手机截图另作人工视觉检查。浏览器自动回归用桩替代 Apple 的外部网络，避免把第三方故障误判为本站回归；另行检查正式域名和 Apple 嵌入地址 HTTP 200。此检查不代表 Apple 在所有地区均提供完整播放，也不代表本站取得第三方歌词授权。

CNAME、.nojekyll、canonical、robots、站点地图及分享图保留，未改 GitHub Pages 配置或 DNS。未新增网站运行依赖。
